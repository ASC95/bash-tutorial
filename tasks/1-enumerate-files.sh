#!/usr/bin/env bash

# - https://mywiki.wooledge.org/ParsingLs

cd "$(dirname "${BASH_SOURCE[0]}")"

# - This task doesn't need to be read before the others, but it's so important that I'm putting it first
